
此 Demo 集合了一些比较常用的布局以及逻辑

BottomNavigationView 开发底部导航栏

DrawerLayout + NavigationView + ActionBarDrawerToggle 开发经典侧边导航栏

DrawerLayout + 自定义侧边栏 开发自定义侧边导航栏

ViewPager 开发导航页

启动页面的 <item name="android:windowBackground">@mipmap/baby</item> style标签

GridLayout 开发计算器样式

CoordinatorLayout + AppBarLayout + Toolbar + TabLayout + ViewPager + Fragment 的经典页面

CollapsingToolbarLayout + ImageView + Toolbar 的页面

以及

沉浸式状态栏开发

Fragment 中菜单的开发

BaseFragment 的封装以及懒加载的实现

RecyclerView 的基本使用方法

RecyclerView + SwipeRefreshLayout 实现下拉刷新

RecyclerView.Adapter 的写法以及上拉加载更多的实现

......等等
 
